"""agentkit — the shared runtime (the Harness) every Agent Foundry agent runs on.

Model + Harness = Agent. The harness provides: core files (SOUL/AGENTS/USER/MEMORY),
skills, tasks, a tool registry with an allowlist, a budget-capped model client,
approval-gated actions, a hash-chained ledger, a scheduler, a doctor, receipted
reports, an A2A agent card, and a Mission Control.
"""
__version__ = "1.0.0"
