fastapi>=0.110
uvicorn[standard]>=0.27   # uvloop + httptools where available (Linux): ~2x throughput vs asyncio + h11
pydantic>=2
ddgs>=9          # real web search (DuckDuckGo), no API key
httpx>=0.27      # tests (TestClient)
pytest>=8
cryptography>=42
